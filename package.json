{
  "name": "game",
  "version": "1.0.0",
  "main": "game.js",
  "scripts": {
    "build": "mkdir -p www && cp index.html style.css game.js www/",
    "cap:sync": "npm run build && npx cap sync",
    "cap:android": "npm run cap:sync && npx cap open android"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@capacitor/android": "^8.4.0",
    "@capacitor/cli": "^7.6.6",
    "@capacitor/core": "^8.4.0"
  }
}
